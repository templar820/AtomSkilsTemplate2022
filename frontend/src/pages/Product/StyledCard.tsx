import styled from 'styled-components';

import { Card, StarIcon } from 'ui-kit';

export const StyledCard = styled(Card)`
  width: 328px;
  margin: 8px;
  min-height: 164px;
  cursor: pointer;
`;

export const StyledStarIcon = styled(StarIcon)`
  color: #ffd700;
`;
