import { css } from '@emotion/core';

const checkboxSquareStyle = css`
    position: absolute;
    right: 15px;
`;

const checkboxCaptionStyle = css`
    width: calc(100% - 80px);
`;

export { checkboxSquareStyle, checkboxCaptionStyle };
